# React 站群管理系统架构分析

## 📋 功能模块分析

### 1. Dashboard（总览看板）
**功能：**
- 总文章数统计
- Google 收录统计
- 今日生成统计
- 活跃站点数
- 数据趋势图表

**对应 Hydra 功能：**
- 已有基础 Dashboard
- 需要添加统计数据 API

### 2. Sites（站点管理）
**功能：**
- 站点列表展示（域名、领域、文章数、状态）
- 搜索功能
- 添加新站点
- 站点状态管理（Active/Building）

**对应 Hydra 功能：**
- 需要新增：站点管理页面
- 数据来源：`config.yaml` 中的 `topology` 配置

### 3. Generator（内容生产工厂）⭐ **核心功能**
**功能：**
- **单篇精修模式**：
  - 文章标题输入
  - 关键词输入
  - 实时预览
  - SEO 评分
  
- **批量站群模式**：
  - 关键词列表输入（每行一个）
  - 目标站点选择
  - AI 语气/风格选择
  - 任务队列管理
  - 批量生成状态监控

**对应 Hydra 功能：**
- 已有：`generate_content()` 方法
- 需要：批量任务队列系统
- 需要：任务状态管理 API

### 4. Templates（AI 模板引擎）
**功能：**
- 模板库管理
- 动态指纹生成（代码混淆）
- 模板预览（视觉/源码对比）
- 生成新变体结构

**对应 Hydra 功能：**
- 已有：Jinja2 模板系统
- 需要：模板指纹混淆功能
- 需要：模板管理界面

### 5. Settings（模型与接口配置）
**功能：**
- 本地模型后端连接配置
- API Endpoint URL
- Model Name
- Max Tokens
- Google SEO 自动优化策略开关

**对应 Hydra 功能：**
- 已有：Settings 页面
- 需要：增强配置选项

---

## 🏗️ 架构对比

### React 组件架构
```
Frontend (React)
├── State Management (useState)
├── API Calls (fetch)
└── UI Components
```

### Hydra 当前架构
```
Backend (FastAPI)
├── Routes (HTML/JSON)
├── Templates (Jinja2)
└── Business Logic (HydraEngine)
```

---

## 🔄 整合方案

### 方案 A：保持 Jinja2 模板（推荐）
**优点：**
- 无需额外构建工具
- 服务端渲染，SEO 友好
- 与现有系统无缝集成

**实现：**
1. 创建新的模板页面（sites.html, generator.html, templates.html）
2. 添加对应的后端 API 路由
3. 使用 HTMX/Alpine.js 实现交互

### 方案 B：独立 React 前端
**优点：**
- 更现代的 UI/UX
- 更好的交互体验

**缺点：**
- 需要构建工具（Vite/Webpack）
- 需要 CORS 配置
- 增加系统复杂度

---

## 📝 实现计划

### Phase 1: 核心功能整合
1. ✅ 站点管理页面（Sites）
2. ✅ 批量生成工厂（Generator）
3. ✅ 模板管理（Templates）

### Phase 2: 增强功能
1. 任务队列系统
2. 模板指纹混淆
3. 统计数据 API

### Phase 3: 优化
1. 实时状态更新（WebSocket）
2. 批量操作优化
3. 性能监控

---

## 🎯 关键技术点

### 1. 批量任务队列
```python
# 需要实现
class BatchQueue:
    - add_task(keyword, site_id, tone)
    - process_queue()
    - get_status(task_id)
    - cancel_task(task_id)
```

### 2. 模板指纹混淆
```python
# 需要实现
class TemplateObfuscator:
    - generate_fingerprint()
    - obfuscate_css_classes()
    - randomize_html_structure()
```

### 3. 站点管理
```python
# 需要实现
- GET /api/sites - 获取站点列表
- POST /api/sites - 添加新站点
- PUT /api/sites/{id} - 更新站点
- DELETE /api/sites/{id} - 删除站点
```

---

## 📊 数据模型

### Site Model
```python
{
    "id": 1,
    "hostname": "example.com",
    "niche": "Technology",
    "articles": 124,
    "status": "Active",
    "template_id": "t3",
    "mask_context": "Industrial Machinery",
    "persona": "Senior Engineer"
}
```

### Task Model
```python
{
    "id": 12345,
    "keyword": "best coffee machine",
    "site_id": 1,
    "status": "pending|processing|done|error",
    "tone": "Professional",
    "created_at": "2024-01-01T00:00:00",
    "result": {...}
}
```

---

## 🚀 下一步行动

1. 创建站点管理页面和 API
2. 实现批量生成工厂
3. 添加模板管理功能
4. 整合到现有系统

